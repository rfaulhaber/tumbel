version: 2
updates:
  # The workflows pin actions by major tag. One grouped pull request a week
  # rather than one per action.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
